@model WebBusinessPromilexApp.Models.Promotion

@{
    ViewData["Title"] = "Usuń Promocję";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="mb-8">
    <h2 class="text-4xl font-bold text-red-500 tracking-tight">Usuń promocję</h2>
    <p class="text-gray-500 mt-1">Czy na pewno chcesz usunąć tę kampanię rabatową?</p>
</div>

<div class="max-w-2xl bg-[#1E1332]/50 border border-red-500/20 rounded-2xl p-8 shadow-[0_0_30px_rgba(239,68,68,0.1)] backdrop-blur-sm">
    
    <div class="bg-[#0D051A] rounded-xl p-6 border border-white/5 mb-8">
        <div class="grid grid-cols-2 gap-4">
            <div>
                <p class="text-[10px] font-bold text-gray-400 uppercase tracking-[0.2em]">Nazwa</p>
                <p class="text-white text-lg font-bold">@Model.Name</p>
            </div>
            <div>
                <p class="text-[10px] font-bold text-gray-400 uppercase tracking-[0.2em]">Zniżka</p>
                <p class="text-[#FF00A0] text-lg font-bold">@Model.DiscountPercentage%</p>
            </div>
            <div class="col-span-2 pt-4 border-t border-white/5">
                <p class="text-[10px] font-bold text-gray-400 uppercase tracking-[0.2em]">Okres trwania</p>
                <p class="text-gray-300">Od: @Model.StartDate <br /> Do: @Model.EndDate</p>
            </div>
        </div>
    </div>

    <form asp-action="Delete" class="flex justify-end items-center space-x-6">
        <input type="hidden" asp-for="Id" />
        <a asp-action="Index" class="text-sm font-bold text-gray-500 hover:text-white transition-colors">
            Anuluj
        </a>
        <button type="submit" class="px-8 py-4 bg-red-600 text-white font-bold rounded-xl shadow-[0_0_20px_rgba(220,38,38,0.4)] hover:shadow-[0_0_30px_rgba(220,38,38,0.6)] hover:bg-red-500 transition-all">
            Usuń kampanię
        </button>
    </form>
</div>